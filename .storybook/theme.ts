import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  brandTitle: 'accent-ui', // This changes the sidebar/header title
  brandUrl: 'https://your-library-homepage.com', // Optional: link to your docs or repo
  brandImage: '', // Optional: add your logo here
});